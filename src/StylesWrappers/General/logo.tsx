import styled from "styled-components";

const Wrapper = styled.main`
  cursor: pointer;
  div p {
    font-size: var(--font);
    color: var(--dark);
    font-weight: 700;
  }
`;

export default Wrapper;
